apply plugin: "java"

sourceCompatibility = 1.7 // this cannot be 1.8 or Android will shit its pants
[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

sourceSets.main.java.srcDirs = [ "src/" ]

eclipse.project {
    name = appName + "-core"
}